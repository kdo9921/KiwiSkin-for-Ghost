<div class="site-header-content">
    <div class="site-title">
        {{#if @site.title}}
        <a href="{{@site.url}}">
            <h1>{{@site.title}}</h1>
        </a>
        {{/if}}
        {{#if @site.description}}
        <a href="{{@site.url}}">
            <p>{{@site.description}}</p>
        </a>
        {{/if}}
    </div>
    </a>
</div>